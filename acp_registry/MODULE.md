# acp_registry

ACP (Agent Client Protocol) registry metadata for editor integrations.

- `agent.json` — package id, version, uvx install spec (`quill-agent[acp]`)
- Published to the ACP registry; version bumped via `scripts/release.py`
